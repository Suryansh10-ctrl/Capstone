apiVersion: v1
kind: Service
metadata:
  name: sandbox-service
  labels:
    app: sandbox-api
spec:
  selector:
    app: sandbox-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP